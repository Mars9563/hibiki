# Dependencies
/node_modules
# NOTE: lock files SHOULD be committed (reproducible installs, needed by `npm ci`).
# /yarn.lock left ignored only because this project uses npm, not yarn.
/yarn.lock

# Environment Variables
.env
.env.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*

# OS and Editor files
.DS_Store
.vscode
